on: 
  workflow_dispatch:
  
env:
  KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}

name: AzureLoginSample

jobs:

  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
   
    - run: |
        kubectl get po -A
